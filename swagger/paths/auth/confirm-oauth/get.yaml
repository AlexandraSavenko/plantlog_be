tags:
  - Auth
summary: get Google URL to sign up User 
operationId: oauth
description: get url which will allow user ot sign up with google account
security:
  - bearerAuth: []
parameters:
  - in: query
    name: code
    required: true
    schema:
      type: string
responses:
  302:
    description: Redirects user after successful OAuth login