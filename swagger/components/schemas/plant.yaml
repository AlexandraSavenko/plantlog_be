      type: object
      required:
        - name
        - description
        - growthForm
        - origin
        - userId
      properties:
        _id:
          type: string
        name:
          type: string
        description:
          type: string
        photo:
          type: string
        growthForm:
          type: string
          enum: [tree, shrub, herb, grass, vine, succulent, fern, moss]
        origin:
          type: string
          enum: [wild, cultivated]
        userId:
          type: string
